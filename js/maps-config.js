/**
 * Google Maps config (frontend).
 *
 * Set your API key below.
 * - Enable: Maps JavaScript API + Places API (and Geocoding API if you want address -> pin)
 * - Restrict the key to your domains/localhost in Google Cloud Console.
 */
window.GOOGLE_MAPS_API_KEY = window.GOOGLE_MAPS_API_KEY || '';

